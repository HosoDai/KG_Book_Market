<% if user.errors.any? %>
 <div class="alert alert-danger">
    <%= user.errors.count %>件のエラーが発生しました。
    <ul>
      <% user.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
    </ul>
  </div>
<% end %>